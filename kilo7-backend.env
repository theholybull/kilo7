# KILO7 backend environment (systemd EnvironmentFile)
# Minimal, deterministic defaults

KILO_ROS_WS=/opt/kilo7/robot/ros_ws

# Optional. Use explicit config path to ensure runtime picks updated settings.
KILO_CONFIG=/opt/kilo7/robot/ros_ws/src/kilo_core/config/kilo.yaml

# Relay kill-path (GPIO)
KILO_RELAY_GPIO_PIN=17
KILO_GPIOD_CHIP=/dev/gpiochip0

# Step 1.5: relay_kill configuration (mode now controlled by kilo.yaml)
# KILO_RELAY_MODE=gpiod  [DEPRECATED: unsupported mode, removed to use yaml config]
KILO_RELAY_KILL_LEVEL_LOW=0

# pigpio connection (matches pigpiod.service)
KILO_PIGPIO_HOST=127.0.0.1
KILO_PIGPIO_PORT=8888
